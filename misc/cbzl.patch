diff --git a/source/cbz/mucbz.c b/source/cbz/mucbz.c
index 2c7bd11..0302f11 100644
--- a/source/cbz/mucbz.c
+++ b/source/cbz/mucbz.c
@@ -116,8 +116,19 @@ cbz_read_zip_entry(cbz_document *doc, cbz_entry *entry, int *sizep)
 
 	if (method == 0)
 	{
-		*sizep = entry->usize;
-		return cdata;
+		fz_buffer *buf;
+		fz_stream *stm;
+		unsigned char *udata;
+		char *tmp = fz_malloc(ctx, entry->csize + 1);
+
+		entry->csize[(char *) memcpy(tmp, cdata, entry->csize)] = 0;
+		stm = fz_open_file(ctx, tmp);
+		buf = fz_read_all(stm, 0);
+		fz_close(stm);
+		fz_free(ctx, cdata);
+		fz_free(ctx, tmp);
+		*sizep = fz_buffer_storage(ctx, buf, &udata);;
+		return udata;
 	}
 
 	if (method == 8)
